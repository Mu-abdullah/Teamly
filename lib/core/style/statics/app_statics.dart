import 'package:flutter/material.dart';

class AppBorderRadius {
  static const BorderRadius smallRadius = BorderRadius.all(Radius.circular(8));
  static const BorderRadius mediumRadius =
      BorderRadius.all(Radius.circular(12));
  static const BorderRadius largeRadius = BorderRadius.all(Radius.circular(16));
}

class AppPadding {
  static EdgeInsets smallPadding = const EdgeInsets.all(10);
  static EdgeInsets mediumPadding = const EdgeInsets.all(20);
  static EdgeInsets largePadding = const EdgeInsets.all(30);
}

class Statics {
  static const double buttonsHeight = 70;
}
